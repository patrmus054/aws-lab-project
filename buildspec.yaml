version: 0.2

phases:
  build:
    commands:
      - chmod +x gradlew
      - ./gradlew assembleDebug
      - ls
      - chmod 777 /codebuild/output/src541901967/src
artifacts:
  files:
    - ./app/build/outputs/apk/app-debug.apk
