version: 0.2

phases:
  build:
    commands:
      - chmod +x gradlew
      - ./gradlew assembleDebug
      - ls
      - chmod 777 /codebuild/output
      - ls ./app/build/outputs/apk/
artifacts:
  files:
    - ./app/build/outputs/apk/app-debug.apk
